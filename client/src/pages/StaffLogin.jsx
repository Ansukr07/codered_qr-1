import {useState} from 'react';
import {Navigate,useNavigate} from 'react-router-dom';
import {api} from '../api';
import {useAuth} from '../auth';
export function StaffLogin(){const {user,setUser}=useAuth();const navigate=useNavigate();const params=new URLSearchParams(location.search);const role=params.get('role')==='admin'?'admin':'volunteer';const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;async function submit(e){e.preventDefault();setBusy(true);setError('');try{const result=await api('/api/auth/staff/login',{method:'POST',body:JSON.stringify({email,password,role})});setUser(result.user);const requested=params.get('returnTo');navigate(requested?.startsWith(`/${role}/`)?requested:'/');}catch(cause){setError(cause.message);}finally{setBusy(false);}}return <main className="login-world"><section className="panel login-card"><p className="eyebrow">STAFF ACCESS</p><h1>{role==='admin'?'Admin':'Volunteer'} login</h1><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error" role="alert">{error}</p>}<button disabled={busy}>{busy?'SIGNING IN…':'SIGN IN'}</button></form></section></main>}
