import {useEffect,useState} from 'react';
import {Link,useParams} from 'react-router-dom';
import {api} from '../api';
export function PublicProfile(){const {username}=useParams();const [profile,setProfile]=useState(null);const [error,setError]=useState('');useEffect(()=>{api(`/api/network/profiles/${encodeURIComponent(username)}`).then(r=>setProfile(r.profile)).catch(e=>setError(e.message));},[username]);if(!profile)return <main className="center"><section className="panel"><h1>{error||'Loading player card…'}</h1><Link to="/">Go to portal</Link></section></main>;return <main className="center"><article className="panel profile-card"><p className="eyebrow">PLAYER CARD · {profile.scanCount} VIEWS</p><div className="avatar">{profile.name[0]}</div><h1>{profile.name}</h1><p className="handle">@{profile.username}</p><p>{profile.bio}</p><p>{profile.teamId||'Individual'} · {profile.track||'CodeRed'}</p><div className="links">{profile.github&&<a href={profile.github} target="_blank" rel="noreferrer">GitHub ↗</a>}{profile.linkedin&&<a href={profile.linkedin} target="_blank" rel="noreferrer">LinkedIn ↗</a>}{profile.portfolio&&<a href={profile.portfolio} target="_blank" rel="noreferrer">Portfolio ↗</a>}</div><Link to="/network">Open networking hub</Link></article></main>}
