import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import {api} from '../api';import {useAuth} from '../auth';
export function Leaderboard(){const {user}=useAuth();const [teams,setTeams]=useState(null);const [error,setError]=useState('');useEffect(()=>{api('/api/participants/leaderboard').then(r=>setTeams(r.leaderboard)).catch(e=>setError(e.message));},[]);return <main className="shell narrow"><header><div><p className="eyebrow">QUEST RANKINGS</p><h1>Team leaderboard</h1></div><Link to="/">Back to hub</Link></header>{error&&<p className="error" role="alert">{error}</p>}{teams===null&&!error&&<section className="panel" aria-busy="true">Calculating rankings…</section>}{teams?.length===0&&<section className="panel empty"><strong>No points awarded yet</strong><p>Rankings appear after volunteers approve the first quest proof.</p></section>}{teams?.length>0&&<ol className="leaderboard" aria-label="Team rankings">{teams.map(team=><li className={team.teamId===user.teamId?'is-you':''} key={team.teamId}><span className="rank">#{team.rank}</span><span><strong>{team.teamId}</strong>{team.teamId===user.teamId&&<small>Your team</small>}</span><span><strong>{team.points} XP</strong><small>{team.completed} quest{team.completed===1?'':'s'}</small></span></li>)}</ol>}</main>}
