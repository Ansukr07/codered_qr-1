import {Link} from 'react-router-dom';
import {useAuth} from '../auth';
export function Dashboard(){const {user,logout}=useAuth();return <main className="shell"><header><div><p className="eyebrow">PLAYER HUB</p><h1>Welcome, {user.name}</h1></div><button className="secondary compact" onClick={logout}>LOG OUT</button></header><section className="hero"><div><span className="badge">{user.participantId||user.role}</span><h2>Your CodeRed journey starts here.</h2><p>Complete your profile before networking, then use your badge for NFC check-ins and connections.</p></div></section><section className="grid"><Link className="action-card" to="/profile"><strong>Player profile</strong><span>Edit your public links and identity →</span></Link><div className="action-card disabled"><strong>NFC badge</strong><span>Next route being migrated</span></div><div className="action-card disabled"><strong>Quest town</strong><span>Next route being migrated</span></div></section></main>}
