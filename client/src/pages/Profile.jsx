import {useEffect,useState} from 'react';
import {Link} from 'react-router-dom';
import {api} from '../api';
import {useAuth} from '../auth';
export function Profile(){const {refresh}=useAuth();const [form,setForm]=useState(null);const [status,setStatus]=useState('');useEffect(()=>{api('/api/participants/me').then(r=>setForm(r.participant)).catch(e=>setStatus(e.message));},[]);if(!form)return <main className="center"><div className="panel">{status||'Loading profile…'}</div></main>;const set=(key,value)=>setForm({...form,[key]:value});async function save(e){e.preventDefault();setStatus('Saving…');try{await api('/api/participants/me',{method:'PATCH',body:JSON.stringify(form)});await refresh();setStatus('Profile saved.');}catch(error){setStatus(error.message);}}return <main className="shell narrow"><Link to="/">← Back to hub</Link><section className="panel"><p className="eyebrow">PLAYER CARD SETUP</p><h1>Build your networking profile</h1><form onSubmit={save}><label>Username<input value={form.username||''} onChange={e=>set('username',e.target.value.toLowerCase())} placeholder="your-name" required/></label><label>Bio<textarea value={form.bio||''} onChange={e=>set('bio',e.target.value)} maxLength="240"/></label><label>GitHub URL<input type="url" value={form.github_profile||''} onChange={e=>set('github_profile',e.target.value)}/></label><label>LinkedIn URL<input type="url" value={form.linkedin_url||''} onChange={e=>set('linkedin_url',e.target.value)}/></label><label>Portfolio URL<input type="url" value={form.portfolio_url||''} onChange={e=>set('portfolio_url',e.target.value)}/></label>{status&&<p role="status" className={status.includes('saved')?'success':'muted'}>{status}</p>}<button>SAVE PLAYER CARD</button></form></section></main>}
