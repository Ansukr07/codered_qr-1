# Leave empty to use the same-origin /api proxy (recommended for Safari/iPhone).
VITE_API_URL=
# Server-side Vercel proxy destination; do not expose this with a VITE_ prefix.
API_ORIGIN=https://your-express-api.vercel.app
